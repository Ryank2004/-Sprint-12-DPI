<!-- Hier word de header geinclude -->
<%- include('./partials/head') %>

<header>
    <h1>
        Exposense
    </h1>
</header>

<section class="intro-text">
    <p><%= tno.intro %></p>
</section>

<div class="svg-container">
    <div class="svg-content">
        <img src="/assets/plaat_mobile_color.svg" alt="plaatje over uitstoot van schadelijke gassen">
        <% chapters.forEach(function(chapter) { %>
            <a href="/chapter/<%= chapter.id %>" class="plus-button" data-id="<%= chapter.id %>"></a>
        <% }) %>
    </div>
</div>

<div id="chapter-display" class="chapter-display"></div>

<!-- Hier word de foot geinclude -->
<%- include('./partials/foot') %>